#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int s[100], k = 0;

int main(void)
{
   int elem;
   char a[5];
   do
   {
      scanf("%s", a);
      if (0 == strcmp(a, "push"))
      {
         scanf("%d", &elem);
         s[k] = elem;
         k++;
         printf("ok\n");
      }
      if (0 == strcmp(a,"pop"))
      {
         printf("%d\n", s[k-1]);
         k--;
      }
      if (0 == strcmp(a, "back"))
         printf("%d\n", s[k-1]);
      if (0 == strcmp(a, "size"))
         printf("%d\n", k);
      if (0 == strcmp(a, "clear"))
      {
         k = 0;
         printf("ok\n");
      }
      if (0 == strcmp(a, "exit"))
         break;
   } while (1);
   printf("bye\n");
   return 0;
}
